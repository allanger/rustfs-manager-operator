apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rustfs-operator-test.fullname" . }}-test-connection"
  labels:
    {{- include "rustfs-operator-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rustfs-operator-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
