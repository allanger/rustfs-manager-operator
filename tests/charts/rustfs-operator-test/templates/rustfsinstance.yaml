apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rustfs-operator-test.serviceAccountName" . }}
  labels:
    {{- include "rustfs-operator-test.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.automount }}
